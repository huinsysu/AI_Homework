need steps: 3, result: fail!
need steps: 2, result: fail!
need steps: 1, result: fail!
need steps: 3, result: fail!
need steps: 3, result: fail!
need steps: 2, result: fail!
need steps: 2, result: fail!
need steps: 2, result: fail!
need steps: 2, result: fail!
need steps: 1, result: fail!
need steps: 1, result: fail!
need steps: 2, result: fail!
need steps: 4, result: fail!
need steps: 1, result: fail!
need steps: 1, result: fail!
need steps: 1, result: fail!
need steps: 3, result: fail!
need steps: 2, result: fail!
need steps: 4, result: fail!
need steps: 2, result: fail!
need steps: 3, result: fail!
need steps: 1, result: fail!
need steps: 2, result: fail!
need steps: 1, result: fail!
need steps: 2, result: fail!
need steps: 4, result: fail!
need steps: 4, result: fail!
need steps: 1, result: fail!
need steps: 2, result: fail!
need steps: 4, result: fail!
success number: 0, average fail steps: 2, need 0.000141 seconds
